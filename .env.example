# Database — PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/pkh_mentoring?schema=public"

# Admin password — used for the /admin/login page
ADMIN_PASSWORD="change-me-to-a-strong-password"

# Application URL — used for generating magic links
NEXT_PUBLIC_APP_URL="http://localhost:3099"

# Resend API key — for sending transactional emails (optional)
RESEND_API_KEY=""

# Slack webhook URL — for enrolment notifications (optional)
SLACK_WEBHOOK_URL=""
